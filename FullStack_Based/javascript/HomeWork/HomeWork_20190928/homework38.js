//38.遍历1到100之间（包含1和100）的所有数字，统计能被5整除的偶数的个数，最后将个数打印在控制台

var count = 0;
for(var i = 1 ; i <= 100 ; i++){

    //偶数并且能被5整除
    if(i % 2 == 0 && i % 5 == 0){
        //计数器自增
        count++;
    }
}

console.log(count);