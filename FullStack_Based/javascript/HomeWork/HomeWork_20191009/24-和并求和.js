// 需求: 计算从1到100临近两个整数的合并依次输出3(1+2),第二次输出5(2+3),最后一次输出199(99+100)

//定义变量 sum 初始值为 0 
var sum = 0;

//创建 for 循环  遍历 从 1- 100
for(var i = 1 ; i < 100 ; i++){

    //将 i 与 i+1 的和 赋值给 sum
    sum = i + (i+1);
    //打印
    console.log(sum+"("+i+"+"+(i+1)+")");
}
